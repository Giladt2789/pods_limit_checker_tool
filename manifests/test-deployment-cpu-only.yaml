apiVersion: v1
kind: Namespace
metadata:
  name: no-memory-limit
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-cpu-only
  namespace: no-memory-limit
  labels:
    app: test-pod
    test-type: cpu-only
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-pod
      test-type: cpu-only
  template:
    metadata:
      labels:
        app: test-pod
        test-type: cpu-only
    spec:
      containers:
      - name: nginx
        image: nginx:alpine
        ports:
        - containerPort: 80
        resources:
          limits:
            cpu: "100m"
